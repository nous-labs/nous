---
title: React Integrations
description: React hooks and providers for Qubic blockchain integration
---

import { Callout } from 'fumadocs-ui/components/callout';

# React Integrations

Complete React integration for Qubic blockchain with context providers, React Query hooks, and WalletConnect support.

## Integration Options

### 1. Basic React Provider

Use the simple `QubicProvider` for direct API access:

```tsx
import { QubicProvider, useQubic } from '@nouslabs/sdk/react';

function App() {
  return (
    <QubicProvider>
      <YourApp />
    </QubicProvider>
  );
}

function Component() {
  const { live, query, archive } = useQubic();
  // Direct client access
}
```

**Best for:** Small apps, prototypes, manual state management

[→ Learn more about React Providers](./react-simple)

### 2. React Query Integration

Use React Query hooks for automatic caching, background refetching, and optimized data fetching:

```tsx
import { QubicQueryProvider, useCurrentTick, useBalance } from '@nouslabs/sdk/react/query';

function App() {
  return (
    <QubicQueryProvider>
      <YourApp />
    </QubicQueryProvider>
  );
}

function Dashboard({ address }) {
  const { data: tick, isLoading } = useCurrentTick();
  const { data: balance } = useBalance(address);
  
  return <div>Tick: {tick?.tickInfo.tick}</div>;
}
```

**Best for:** Production apps, complex state, real-time data, large applications

[→ Learn more about React Query Hooks](./react-query)

### 3. Smart Contract Hooks

Ready-made hooks for all Qubic smart contracts (QX, Qswap, Qearn, etc.):

```tsx
import { useQXFees, useQswapPoolBasicState } from '@nouslabs/sdk/react/query';

function TradingDashboard() {
  const { data: qxFees } = useQXFees();
  const { data: pool } = useQswapPoolBasicState(issuer, assetName);
  
  return <div>Trade Fee: {qxFees?.tradeFee}</div>;
}
```

**Best for:** DeFi apps, trading interfaces, contract interactions

[→ Learn more about Contract Hooks](./react-query-contracts)

## Quick Comparison

| Feature | Basic Provider | React Query |
|---------|----------------|-------------|
| Setup Complexity | Simple | Moderate |
| Automatic Caching | ❌ | ✅ |
| Background Refetch | ❌ | ✅ |
| Loading States | Manual | Automatic |
| Error Handling | Manual | Built-in |
| Request Deduplication | ❌ | ✅ |
| Optimistic Updates | Manual | Built-in |
| DevTools | ❌ | ✅ |
| Bundle Size | Smaller | Larger |

## WalletConnect Integration

**Built-in Provider**: The SDK includes `WalletConnectProvider` and `useWalletConnect` hook - no manual setup required!

[→ Complete Next.js + WalletConnect + React Query Guide](./nextjs-walletconnect)

Simple integration for wallet connections:

```tsx
import { WalletConnectProvider, useWalletConnect } from '@nouslabs/sdk/react';

function WalletButton() {
  const { connect, disconnect, isConnected, accounts } = useWalletConnect();
  const [showQR, setShowQR] = useState(false);
  const [uri, setUri] = useState('');
  
  const handleConnect = async () => {
    try {
      const { uri, waitForApproval } = await connect();
      setUri(uri);
      setShowQR(true);
      
      // Accounts update automatically after approval!
      await waitForApproval();
      setShowQR(false);
    } catch (error) {
      console.error('Connection failed:', error);
      setShowQR(false);
    }
  };
  
  if (isConnected) {
    return (
      <div>
        <p>{accounts[0].identity.slice(0, 8)}...</p>
        <button onClick={disconnect}>Disconnect</button>
      </div>
    );
  }
  
  return (
    <>
      <button onClick={handleConnect}>Connect Wallet</button>
      
      {showQR && uri && (
        <div className="qr-modal">
          <img src={`https://api.qrserver.com/v1/create-qr-code/?data=${uri}`} />
          <button onClick={() => setShowQR(false)}>Cancel</button>
        </div>
      )}
    </>
  );
}

function App() {
  return (
    <WalletConnectProvider 
      options={{
        projectId: 'YOUR_PROJECT_ID',
        metadata: { name: 'Your App', /* ... */ }
      }}
    >
      <YourApp />
    </WalletConnectProvider>
  );
}
```

<Callout type="success">
  **Simplified & Automatic**: Use `connect()` and `disconnect()` helpers - accounts update automatically after approval!
</Callout>

## When to Use Each Approach

### Use Basic React Provider When:
- ✅ Building a prototype or small app
- ✅ You want minimal dependencies
- ✅ You're comfortable with manual state management
- ✅ You don't need automatic refetching
- ✅ Simple read operations only

### Use React Query When:
- ✅ Building a production application
- ✅ You need real-time data updates
- ✅ Complex data fetching patterns
- ✅ Multiple components accessing same data
- ✅ You want automatic caching and optimization
- ✅ Working with smart contracts extensively

## Next Steps

<Cards>
  <Card title="React Providers" href="/docs/integrations/react-simple">
    Basic React context providers and hooks
  </Card>
  <Card title="React Query Hooks" href="/docs/integrations/react-query">
    Complete guide to React Query integration
  </Card>
  <Card title="Contract Hooks" href="/docs/integrations/react-query-contracts">
    Pre-built hooks for all Qubic smart contracts
  </Card>
  <Card title="Next.js + WalletConnect" href="/docs/integrations/nextjs-walletconnect">
    Complete guide to integrating WalletConnect in Next.js apps
  </Card>
</Cards>

## Installation

All React integrations are included in the main package:

```bash
npm install @nouslabs/sdk

# For React Query integration, also install:
npm install @tanstack/react-query
```

## TypeScript Support

Both approaches have full TypeScript support with automatic type inference:

```tsx
import { useQubic } from '@nouslabs/sdk/react';
import { useBalance } from '@nouslabs/sdk/react/query';

// Types are automatically inferred
function Component() {
  const { live } = useQubic();
  // live is typed as QubicLiveClient
  
  const { data } = useBalance('address');
  // data is typed as GetBalanceResponse | undefined
}
```
