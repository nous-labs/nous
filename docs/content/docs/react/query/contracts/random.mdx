---
title: Random hooks
description: React Query hooks for Random.
---

# Random

Randomness oracle outputs and entropy feeds.

## Available hooks

```ts
useRandomQuery()
```

Each hook lives in `@nouslabs/react/query` and wraps the underlying `@nouslabs/sdk` helpers.
