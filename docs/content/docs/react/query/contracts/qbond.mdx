---
title: Qbond hooks
description: React Query hooks for Qbond.
---

# Qbond

Bond issuance and state.

## Available hooks

Hooks for this contract are not yet published. Use the low-level `useContractQuery` builder until they land.
Each hook lives in `@nouslabs/react/query` and wraps the underlying `@nouslabs/sdk` helpers.
